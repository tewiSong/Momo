下面只列**实现上有问题、而且会直接造成你现在这组结果**的地方。

| 代码位置                                                                                   | 具体问题                                                                                                                                                                                               | 为什么会导致你现在的现象                                                                                | 具体修复方案                                                                                                                                                                                    |                                                                                      |                                                                                               |
| -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------- |
| `momo/train/pretrain.py`，训练日志里 `top1_nn_hit` 的统计                                       | 现在还是拿 `teacher_nn_index` 算命中率，不是几何版该看的 `gt_nn_index`。                                                                                                                                              | 所以你看到 `top1_nn_hit=0` 基本是**日志错**，不是模型一定全错。这个指标现在没诊断价值。                                      | 把 `nn_idx = router_info.get('teacher_nn_index', None)` 改成优先取 `gt_nn_index`；没有 `gt_nn_index` 再回退。也就是：`nn_idx = router_info.get('gt_nn_index', router_info.get('teacher_nn_index', None))`。 |                                                                                      |                                                                                               |
| `momo/train/pretrain.py`，MMM 分支里 `k_target = router_info.get('teacher_nn_index')`      | MMM 还在用 `teacher_nn_index` 当分类目标。                                                                                                                                                                  | 主损失已经改成几何版了，但辅助任务还在推 teacher 目标，等于**一边往几何目标拉，一边往 teacher 目标拉**，router 很容易更尖锐、更塌缩。           | MMM 目标统一改成 `gt_nn_index`。也就是：`k_target = router_info.get('gt_nn_index')`，并断言它存在。不要再让 teacher 参与 MMM 目标。                                                                                   |                                                                                      |                                                                                               |
| `momo/train/pretrain.py`，`assert teacher.enabled=True` 和 `require_pos=teacher.enabled` | 预训练入口仍然把 teacher 当硬依赖。                                                                                                                                                                             | 你现在理想方案已经是“显式几何标签主监督”，teacher 只该是可选辅助。这里还强绑 teacher，会让训练逻辑和方案不一致，也让很多日志/辅助项继续走 teacher 旧路径。 | 去掉硬断言；`require_pos` 不要再由 `teacher.enabled` 决定。改成：几何版预训练默认可不依赖 teacher；只有明确打开 teacher 辅助项时才要求 `pos`。                                                                                       |                                                                                      |                                                                                               |
| `momo/train/pretrain.py`，`commit_ramp_steps` 和 `proto top-k schedule` 的组合              | 代码里 `commit` 是慢升温，但 prototype 路由会更早收敛到 `final_k=1`。你贴的配置就是 `commit_ramp_steps=200000`，而 `topk_schedule.stage2_until=160000`。对应代码就是先按 `commit_ramp_steps` 升温，再按 `stage2_until/final_k` 把路由切到 Top-1。 | 这会出现“**还没学会贴近正确原型，就先被强制只能选 1 个原型**”。结果就是 CE 降、熵降、有效码暴跌、commit 不降。                           | 把 `final_k=1` 的开始时间推迟到 `commit` 基本稳定之后。最简单做法：`stage2_until >= commit_ramp_steps`，甚至再往后推；在 `commit` 明显下降前，一直保留 `Top-4`。                                                                    |                                                                                      |                                                                                               |
| `momo/train/losses.py`，`CE + KL` 同时强压 router 去追最近原型                                    | Codex 回执里，`vq` 用 `gt_nn_index` 和 `zgt_proj`，同时你还有 `CE(logits_code, gt_nn_index)` 与 `KL(p_gt                                                                                                        |                                                                                             | p_code)` 这一套监督。                                                                                                                                                                           | 这会把 router 很快推成**又尖又偏**：CE 负责把分布压尖，KL 负责继续追几何分布；如果码本早期还不稳，就会集中到少数码上，形成你看到的 collapse。 | 不要同时重压两项。先保留 `KL`，把 `CE` 降得很低或延后打开；或者把 `p_gt` 温度调高，让目标分布更软。实操上先把 `ce_weight` 降一个量级，再看有效码是否回升。 |
| `momo/models/gin_motif_vqmoe.py` + `momo/train/pretrain.py`，码本初始化                      | `Codebook` 默认是随机初始化；而训练脚本里只有在 `codebook_init.name == 'kmeans'` 时才会做聚类初始化。你现在给的配置是 `random`。                                                                                                        | 随机码本 + 早期最近邻监督，最容易把一堆样本吸到少数几个“碰巧近”的码上，形成伪簇塌缩。                                               | 不要用随机码本。用 `z3d_gt` 或 `zgt_proj` 做一次 k-means 初始化 codebook，再开始训练。几何版里这是高优先级修复，不是可选优化。                                                                                                       |                                                                                      |                                                                                               |
| `momo/models/gin_motif_vqmoe.py`，teacher 旧路径仍保留在 forward 里                             | 模型前向里还会计算 `teacher_z / p_t / teacher_nn_index` 并塞进 `router_info`。                                                                                                                                  | 这本身不是致命 bug，但会让训练脚本很容易“顺手”继续引用 teacher 旧字段。你现在看到的日志/MMM 就是这样被旧字段污染的。                        | 几何版单独清理：如果 teacher 不是主链路，就把 `teacher_nn_index/p_t` 从默认 router_info 输出里去掉，或者显式放进 `aux_teacher_*` 命名，避免训练脚本误用。                                                                              |                                                                                      |                                                                                               |

结论很直接：

**你现在的问题主要在训练脚本残留了 teacher 旧路径、以及路由调度太激进。**
不是“prototype + Delta”这个方案错，而是这几个实现点把 router 训塌了。
